import React from 'react';

import styles from './Achievements.module.scss';

const Achievements = () => {
    return (
        <div>
            
        </div>
    );
};

export default Achievements;